# Scripts

This directory is reserved for small repository maintenance scripts.

Rules:

- Scripts must be deterministic and safe to run locally.
- Do not put one-off private data processing scripts here.
- Prefer CLI commands inside `src/change_plate_next/interfaces/cli` for product behavior.
